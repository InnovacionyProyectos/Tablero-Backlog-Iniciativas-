name: Actualizar datos de iniciativas
on:
  repository_dispatch:
    types: [torre-control-update]

permissions:
  contents: write

jobs:
  build-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Guardar payload recibido
        run: echo '${{ toJson(github.event.client_payload) }}' > payload.json
      - name: Generar data.json e iniciativas.xlsx
        run: |
          pip install pandas openpyxl
          python scripts/build_excel.py
      - name: Commit y push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json iniciativas.xlsx
          git diff --staged --quiet || git commit -m "Actualiza data.json e iniciativas.xlsx desde Torre de Control"
          git push
